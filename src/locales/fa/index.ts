import containers from "./containers.json";
import defaults from "./defaults.json";
import errors from "./errors.json";
import pages from "./pages";

const faLocale = { containers, pages, defaults, errors };
export default faLocale;
