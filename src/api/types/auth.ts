export interface UserActivity {
  date: string;
  active: boolean;
  time_spent: number;
}
export interface TeacherItem {
  id: number;
  name: string;
  avatar: string;
  username: string;
  sex: any;
}
